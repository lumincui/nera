# NeraCore

Shared Swift models for the MVP flow.

This package intentionally uses `HardcodedPairing.dev` while pairing is out of scope.

The dev pairing uses APNs `sandbox`. TestFlight and App Store builds must use
APNs `production` when server push is added.
